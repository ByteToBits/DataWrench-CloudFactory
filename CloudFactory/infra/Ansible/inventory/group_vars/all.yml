---
ansible_user: "{{ lookup('env', 'ANSIBLE_SSH_USER') | default('ansible', true) }}"
ansible_ssh_private_key_file: ~/.ssh/cf_homelab
ansible_become: true
ansible_become_method: sudo

timezone: Asia/Singapore
