---
# Template for vars/patroni.vault.yml — the real file is ansible-vault encrypted and gitignored (*.vault.*).
#   cp vars/patroni.example.yml vars/patroni.vault.yml
#   (fill in values: openssl rand -base64 32)
#   uv run ansible-vault encrypt vars/patroni.vault.yml
patroni_superuser_password: CHANGE_ME
patroni_replication_password: CHANGE_ME
patroni_rewind_password: CHANGE_ME
patroni_restapi_password: CHANGE_ME
